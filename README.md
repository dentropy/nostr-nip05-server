# nostr-nip05-server


``` bash

deno run -A --unstable-kv --unstable-broadcast-channel ./test.js

```

#### DD-schema.json Template

``` JSON

{
    "app_names" : [],
    "app_version" : "0.0.1",
    "tags" : [""],
    "authors" : [""],
    "schemas" : [
        {
            "schema_name" : "",
            "rxdb_json" : {}
        }
    ]
}

```
