export async function claim_coupon(MyDDSchema, ddroot, req, res, nostr_content_json){
        // Check coupon exists
        // Check pki for private key
        // Check balance of token using public key (did)
        // Sign transaction useing private key
        // Do the transfater
        return({
            "status": "error",
            "error": `claim_coupon is not yet implimented`
        })
}